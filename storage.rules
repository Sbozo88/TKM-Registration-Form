service firebase.storage {
  match /b/{bucket}/o {
    match /cvs/{fileName} {
      allow read: if true;
      allow write: if request.resource.size < 5 * 1024 * 1024;
    }
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
